pipeline {
    agent any
    stages {
        stage('Compile') {
           steps {
		        sh "mvn compile"
           } 
        }
        stage ('UnitTest') {
            steps {
		sh "mvn test"
            }
        }
        stage ('Package') {
            steps {
		    sh "mvn package"
            }
        }
    }
}
